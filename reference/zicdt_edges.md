# Extract the learned edges as a data frame

Extract the learned edges as a data frame

## Usage

``` r
zicdt_edges(object)
```

## Arguments

- object:

  A `"zicdt"` object.

## Value

A data frame with columns `parent` and `child` (node names), one row per
directed edge \\parent \to child\\.
