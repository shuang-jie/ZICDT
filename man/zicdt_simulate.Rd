% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{zicdt_simulate}
\alias{zicdt_simulate}
\title{Simulate zero-inflated compositional data on a directed tree}
\usage{
zicdt_simulate(
  true_G,
  d = 50,
  n = 100,
  omega1 = 0.95,
  precision = 20,
  zero_inflate = 0,
  seed = NULL
)
}
\arguments{
\item{true_G}{A \code{p x p} 0/1 adjacency matrix of the true directed tree,
where \code{true_G[k, j] == 1} means \eqn{k \to j} (k is the parent of j).
Each column must have at most one non-zero entry (at most one parent).}

\item{d}{Integer vector of length \code{p} giving each node's compositional
dimension, or a single integer used for all nodes.}

\item{n}{Number of samples (subjects).}

\item{omega1}{Parental-influence weight (\eqn{\omega_1}); \eqn{\omega_0 = 1 - \omega_1}.}

\item{precision}{Dirichlet precision \eqn{\kappa} controlling dispersion.}

\item{zero_inflate}{Optional fraction in [0, 1); if > 0, that fraction of
entries in each row is randomly set to exactly zero and the row renormalized,
to mimic zero inflation.}

\item{seed}{Optional integer seed.}
}
\value{
A list with \code{data} (a length-\code{p} list of \code{n x d_j}
  matrices) and \code{true_G} (the adjacency matrix used).
}
\description{
Generates data from the paper's model: for each edge \eqn{k \to j} the child
conditional mean is \eqn{\omega_0 \eta_j + \omega_1 M_{jk} x^{(k)}} with a
column-stochastic transition matrix \eqn{M_{jk}}; observations are drawn as
\eqn{x^{(j)} \sim \mathrm{Dir}(\kappa \cdot \widehat{x}^{(j)})}.
}
