% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zicdt.R
\name{zicdt_cv}
\alias{zicdt_cv}
\title{Select the penalty by cross-validation and fit the final structure}
\usage{
zicdt_cv(
  data,
  alpha_grid = NULL,
  K = 5,
  em_max_iter = 20000,
  em_eps = 1e-08,
  use_parallel = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{data}{A named list of \code{n x d_j} compositional matrices (see \code{\link{zicdt_fit}}).}

\item{alpha_grid}{Numeric vector of candidate penalties. If \code{NULL}, a
default grid \code{c(seq(0.01,0.09,0.01), seq(0.1,1,0.1))} is used.}

\item{K}{Number of cross-validation folds (default 5).}

\item{em_max_iter, em_eps}{EM iteration cap and convergence tolerance.}

\item{use_parallel}{Parallelize folds (default \code{FALSE}; sequential is the
portable, package-safe path).}

\item{verbose}{If \code{FALSE} (default), suppress the engine's console output.}
}
\value{
An object of class \code{"zicdt"} as in \code{\link{zicdt_fit}}, with
  additional elements \code{cv} (the per-penalty CV table) and \code{params}
  (the learned edge parameters of the final model).
}
\description{
Runs K-fold cross-validation over a grid of penalties, picks the penalty that
minimizes out-of-sample KL loss (with tie-breaking toward sparser trees), and
refits the final directed tree/forest on all data at the selected penalty.
}
