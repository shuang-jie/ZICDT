% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zicdt.R
\name{zicdt_fit}
\alias{zicdt_fit}
\title{Fit a directed tree/forest at a fixed penalty}
\usage{
zicdt_fit(
  data,
  alpha,
  penalty_rate = 1,
  em_max_iter = 5000,
  em_eps = 1e-06,
  verbose = FALSE
)
}
\arguments{
\item{data}{A named list of \code{n x d_j} matrices, one per node; each row is
a composition (nonnegative, sums to 1). All matrices must share the same
number of rows \code{n}.}

\item{alpha}{Edge penalty (larger \code{alpha} -> sparser structure).}

\item{penalty_rate}{Per-edge complexity penalty (default 1).}

\item{em_max_iter, em_eps}{EM iteration cap and convergence tolerance.}

\item{verbose}{If \code{FALSE} (default), suppress the engine's console output.}
}
\value{
An object of class \code{"zicdt"}: a list with \code{adjacency}
  (\eqn{k \to j} when \code{adjacency[k, j] == 1}), \code{n_edges},
  \code{n_roots}, \code{roots}, \code{is_tree}, \code{score}, \code{alpha},
  and \code{graph} (an \pkg{igraph} object).
}
\description{
Learns the globally optimal directed tree (or forest) over compositional nodes
at a given edge penalty \code{alpha}, using EM edge fitting and the
Chu-Liu/Edmonds algorithm.
}
